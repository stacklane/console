
#
# Largest Stacklane concept, which is an entry point for bringing together other concepts.
#
# ----------

org?: Org

name: # For UI use
  type: string
  min: 2

icon: svg-icon # For UI use

simple?: ProjectVersion # If linked, then "simple mode" -- for testing *and* production == no granular ProjectVersion's

account?: Account # Responsible for billing of this Project

# ------------
#
# The following are primarily used to initialize settings for *new* ProjectDeployment's.
# They are not a source-of-truth for what is within any given ProjectDeployment.
#
# ------------

region:
  type: options
  init: true
  values:
    us: United States
    eu: European Union

users?: # Legacy null
  type: namespace
  init: true

data:
  type: namespace
  init: true
