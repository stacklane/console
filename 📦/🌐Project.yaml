
#
# Largest Stacklane concept, which is an entry point for bringing together many other concepts.
#
# ----------

org?: Org

name: # For UI use
  type: string
  min: 2

icon: svg-icon # For UI use

simple?: ProjectVersion # If linked, then "simple mode" -- for testing *and* production == no granular ProjectVersion's

account?: Account # Responsible for billing of this Project (when deployed)

# ------------

region:
  type: options
  init: true
  values:
    us: United States
    eu: European Union

data:
  type: namespace
  init: true


# DEPRECATED

mapping?: Mapping

source?: source

properties?: map
